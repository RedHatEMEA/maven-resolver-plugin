import java.io.*;
import java.util.*;

import org.codehaus.plexus.util.*;

File file = new File( localRepositoryPath, "some" );
System.out.println( "Deleting local repository group [" + file + "]");
FileUtils.deleteDirectory( file );

return true;
